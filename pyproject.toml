[project]
name = "proxy-service"
version = "0.1.0"
description = "A reverse proxy service built with FastAPI"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.0",
    "httpx>=0.28.1",
    "uvicorn>=0.45.0",
]
